[Unit]
Description=ttfx matrix audio-reactive desktop background
After=graphical-session.target

[Service]
Type=simple
ExecStart=/bin/bash -c 'set -a; source <(systemctl --user show-environment); set +a; exec setsid /home/avillagran/Work/omarchy-plugins/omarchy-ttfx-background/bin/ttfx_bg.sh'
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
